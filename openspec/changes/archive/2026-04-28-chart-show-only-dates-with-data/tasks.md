## 1. 圖表資料轉換調整

- [x] 1.1 在 `chartData` 轉換層新增有效資料過濾（`date` 有效且 `close` 可解析且 `> 0`）
- [x] 1.2 以同一份過濾後資料同時產生 labels 與 series，確保索引對齊
- [x] 1.3 無有效資料時回傳空 labels/series，不補日期佔位

## 2. 圖表整合與測試

- [x] 2.1 調整 `TvChart` 與頁面使用流程，僅使用過濾後圖表資料
- [x] 2.2 更新 `chartData.spec.ts`：混合資料、全無效資料、索引對齊案例
- [x] 2.3 更新 `TvChart.spec.ts`：確認只渲染有資料日期與對應點位
