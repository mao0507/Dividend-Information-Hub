## 1. 資料與狀態模型

- [x] 1.1 新增 `normalizeChartData()` 並定義可渲染、空資料、錯誤資料的輸出型別
- [x] 1.2 在圖表流程導入統一狀態列舉（`loading`/`ready`/`empty`/`error`）
- [x] 1.3 將頁面資料轉換邏輯改為透過正規化入口，移除重複判斷

## 2. 圖表元件穩定化

- [x] 2.1 調整 `TvChart` 初始化流程：僅在容器尺寸有效時建立圖表
- [x] 2.2 導入 ResizeObserver 監聽與安全重試，避免 0 尺寸初始化失敗
- [x] 2.3 加入 empty/error 可視化狀態，禁止無訊息空白畫面

## 3. 頁面整合與驗證

- [x] 3.1 更新儀表板與個股頁圖表掛載流程，確保資料晚到也可觸發渲染
- [x] 3.2 補齊 `TvChart` 測試：成功渲染、空資料、錯誤資料、延後初始化
- [x] 3.3 以實際頁面流程驗證圖表在切頁/縮放/重新進入時持續可見
