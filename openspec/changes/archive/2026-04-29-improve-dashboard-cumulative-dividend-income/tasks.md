## 1. API 契約與資料定義對齊

- [x] 1.1 盤點 Dashboard 彙總 API 的累積股息收入欄位，補齊 `asOf` 與狀態判定所需資料
- [x] 1.2 實作或調整後端 DTO/型別驗證，確保缺欄位與異常格式可被明確辨識
- [x] 1.3 新增後端測試案例，覆蓋 ready、empty、stale、error 四種狀態輸出

## 2. 前端卡片資料轉換與顯示邏輯

- [x] 2.1 更新 Dashboard 卡片轉換函式，將累積股息收入納入統一資料模型（`title/displayValue/state/asOf`）
- [x] 2.2 實作累積股息收入中性值策略，禁止用 `0` 代表缺值或錯誤
- [x] 2.3 對齊累積股息收入與 Hero/其他卡片的 `asOf` 基準，基準不可用時同步降級
- [x] 2.4 更新 `DashboardPage.vue` 的狀態文案與渲染條件，確保異常情境可辨識

## 3. 測試與驗證

- [x] 3.1 新增/調整前端單元測試，驗證累積股息收入在完整、缺值、過期、錯誤情境的顯示結果
- [x] 3.2 驗證累積值只採計已確認入帳資料，不含預估或未確認資料
- [x] 3.3 執行 dashboard 相關測試與 lint，確認既有四卡與 Hero 行為無回歸
