## 1. 後端查詢鏈路修正（DB-first）

- [x] 1.1 盤點 `backend/src/stock` 現有圖表/行情查詢路徑，統一先查 DB 再判斷 fallback
- [x] 1.2 在服務層加入受控回補條件（開關、同步狀態、日期範圍）並封裝為可測函式
- [x] 1.3 新增結構化缺漏狀態碼與訊息（`MISSING_IN_DB`、`SYNC_NOT_READY`、`SOURCE_UNAVAILABLE`）

## 2. 同步狀態與資料驗證整合

- [x] 2.1 擴充同步狀態讀取欄位（最近成功交易日、最後執行時間、狀態）供查詢流程使用
- [x] 2.2 將 `stock-price-data-validation` 的日期正規化與狀態分類規則共用到查詢路徑
- [x] 2.3 補齊 DB 缺漏/非交易日/同步未就緒之可追蹤診斷內容

## 3. 前端圖表容錯與提示

- [x] 3.1 調整 `frontend/src/pages/DashboardPage.vue` 圖表資料映射為逐點容錯策略
- [x] 3.2 新增資料品質提示（缺失點位數、狀態訊息），避免整圖空白無原因
- [x] 3.3 驗證不同時間區間切換（1W/1M/3M/6M/1Y/MAX）在部分資料情境仍可渲染

## 4. 測試與驗證

- [x] 4.1 新增後端測試：DB 命中不觸發外部來源
- [x] 4.2 新增後端測試：DB 缺漏且允許回補時可回填並回傳資料
- [x] 4.3 新增後端測試：DB 缺漏且不同步條件時回傳 `SYNC_NOT_READY`
- [x] 4.4 新增前端測試：單筆壞資料不會導致整段序列為空
- [x] 4.5 執行整體驗證（backend test + frontend 渲染檢查）並更新文件
